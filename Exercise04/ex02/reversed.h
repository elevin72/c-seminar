
/**
 * Accepts a sentence and prints the words in reverse order.
 * Ignores multiple whitespaces and treats them as one space.
 * Does this with zero allocation or recursion
 *
 * @param string A sentence
 */
void PrintReversed(const char* sentence);
