#ifndef SUM_H
#define SUM_H

#include <stddef.h>

/**
 * Accepts an array of integers and sums it
 *
 * @param integers An array of integers
 * @param size The number of integers in the array
 *
 * @return The sum of all integers in the array
 */
int sum(int *integers, size_t size);

#endif // SUM_H
